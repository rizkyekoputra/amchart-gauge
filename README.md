# React AmAngularGauge of AmCharts

A basic example of a pure AmAngularGauge created with react.

## Getting started

Once you clone the repo you can run:

```
$ npm install
$ npm start
```

The app will start on `localhost:3000`.
